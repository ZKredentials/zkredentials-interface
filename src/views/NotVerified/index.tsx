import { NotVerifiedContainer, NotVerifiedTitle } from "./style";

const NotVerified = () => {
  return (
    <NotVerifiedContainer>
      <NotVerifiedTitle>You have yet to verify with WorldID</NotVerifiedTitle>
    </NotVerifiedContainer>
  );
};

export default NotVerified;
