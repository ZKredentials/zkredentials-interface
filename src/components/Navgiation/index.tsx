import { NavigationContainer } from "./style";
import Wallet from "../Wallet";

const Navigation = () => {
  return (
    <NavigationContainer>
      <Wallet />
    </NavigationContainer>
  );
};

export default Navigation;
